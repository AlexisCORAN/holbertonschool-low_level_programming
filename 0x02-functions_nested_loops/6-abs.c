#include "holberton.h"

/**
* _abs - check the absolute value of an integer
*
* @n: the character to be checked
*
* Return: n show absolute value
*/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
